class Expenditure {
    constructor(id, name, amount, type, date, isCompleted) {
        this.id = id;
        this.name = name;
        this.amount = amount;
        this.type = type;
        this.date = date;
        this.isCompleted = isCompleted;
    }
}

export default Expenditure;
