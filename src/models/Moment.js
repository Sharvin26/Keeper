class Moment {
    constructor(id, title, image, date, description) {
        (this.id = id),
            (this.title = title),
            (this.image = image),
            (this.date = date),
            (this.description = description);
    }
}

export default Moment;
