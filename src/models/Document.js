class Document {
    constructor(id, label, pdfUri, date) {
        (this.id = id),
            (this.label = label),
            (this.pdfUri = pdfUri),
            (this.date = date);
    }
}

export default Document;
