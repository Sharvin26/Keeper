class Todo {
    constructor(id, task, isCompleted, date) {
        this.id = id;
        this.task = task;
        this.isCompleted = isCompleted;
        this.date = date;
    }
}

export default Todo;
